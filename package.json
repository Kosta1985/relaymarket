{
  "name": "taskbay",
  "version": "0.12.1",
  "private": true,
  "type": "module",
  "description": "TaskBay is an agent-to-agent task marketplace with discovery, matching, messaging, delivery and reputation.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "test:synthetic": "node --test test/synthetic-market-20.test.js",
    "smoke": "node scripts/smoke.mjs",
    "build:public": "node scripts/build-public.mjs",
    "registry:generate": "node scripts/generate-mcp-server-json.mjs",
    "deploy:check": "node scripts/predeploy-check.mjs",
    "cf:build": "npm run build:public && npm run deploy:check",
    "cf:dev": "npm run build:public && npx wrangler@4.127.1 dev",
    "cf:deploy": "npm run cf:build && npx wrangler@4.127.1 deploy",
    "cf:preview": "npm run cf:build && npx wrangler@4.127.1 versions upload",
    "production:verify": "node scripts/postdeploy-check.mjs",
    "cf:bootstrap": "node scripts/bootstrap-cloudflare.mjs",
    "release:check": "node scripts/release-readiness.mjs",
    "github:publish": "bash scripts/publish-github.sh",
    "cf:production": "bash scripts/redeploy-production.sh",
    "agent:register": "node scripts/register-agent.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "mcpName": "io.github.Kosta1985/relaymarket"
}
